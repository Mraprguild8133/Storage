version: '3.8'

services:
  telegram-file-bot:
    build: .
    container_name: telegram-file-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
